def calculator(money,bar):
    n = 0
    n = money//bar
    change = money - n*bar
    left=""
    left = "The number of bars that can be bought is %f. The change that will be left over is %f." %(n,change)
    return left
money = float(input("Enter the total money:"))
bar = float(input("Enter the price of one chocolate bar:"))
print(calculator(money,bar))
'''
Enter the total money:67
Enter the price of one chocolate bar:6
The number of bars that can be bought is 11. The change that will be left over is 1.
'''
